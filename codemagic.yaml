workflows:
  android-release:
    name: Android Release
    environment:
      android_signing:
        - keystore_credentials
    scripts:
      - name: Flutter build
        script: |
          flutter pub get
          flutter build apk --release
    artifacts:
      - build/app/outputs/flutter-apk/*.apk
